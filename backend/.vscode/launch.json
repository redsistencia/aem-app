{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "FastAPI (uvicorn)",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "main:app",
                "--reload",
                "--app-dir",
                "src"
            ],
            "cwd": "${workspaceFolder}",
            "justMyCode": true
        }
    ]
}
